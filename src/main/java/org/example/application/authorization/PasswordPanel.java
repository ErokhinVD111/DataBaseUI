package org.example.application.authorization;

public class PasswordPanel extends Panel{
    public PasswordPanel(String label, int fieldSize) {
        super(label, fieldSize);
        initializePanel();
    }
}
